# Diary: Clean up global otel state leakage in jobs runner tests

`/jobs/runner_test.go` sets the global OpenTelemetry tracer provider and propagator and never restores them, so state leaks into other tests in the package run. This was spotted during the issue 197 work (see `/docs/diary/2026-08-24-public-endpoint-tracing.md`, Step 2 follow-ups) and left alone there as pre-existing and out of scope. The fix is surgical: register cleanup so the globals are contained, following the pattern established by `useTraceContextPropagator`/`usePropagators` in `/http/otel_test.go` — noting the sharp edge found there that the default global propagator is a delegator, so a naive "restore the previous value" does not actually undo the installation.

## Step 1: Requirements

**Author:** main

### Prompt Context

**Verbatim prompt:** "I've checked 2, all good. Check in Honeycomb too if you like. Spawn a builder to fix 1, keep it concise and surgical."

**Interpretation:** Verify the merged issue-197 fix in production telemetry (done: all 35 `POST /mcp` spans in the last 36h are trace roots with exactly one link), then delegate a minimal fix for the test hygiene issue.

**Inferred intent:** Tests should not leak global state; the fix should not grow into a refactor.

### What I did

Confirmed the production fix in Honeycomb (c6, 36h: `POST /mcp` volume 35, parented 0, `span.num_links` 1 across the board on build `0ec43200`). Wrote requirements for a builder: contain the global tracer provider and propagator mutations in `/jobs/runner_test.go` with test cleanup, mirroring the `http` package's approach and its delegator caveat.

### Why

Leaked globals make test outcomes order-dependent, which `-shuffle` turns into flakes.

### What worked

The Honeycomb verification took one query; `span.num_links = 1` and `parented = 0` on every span is exactly the designed behavior.

### What didn't work

Nothing failed in this step.

### What I learned

Nothing new this step; the delegator caveat was learned during the issue-197 work.

### What was tricky

Nothing; scope is deliberately minimal.

### What warrants review

The builder's diff should touch only test code in `/jobs`, and `go test -shuffle on ./...` should pass.

### Future work

None anticipated beyond the builder's step.

## Step 2: Implement the cleanup

**Author:** jobs-test-cleanup

### Prompt Context

**Verbatim prompt:** "Small, surgical task — no scope growth. Contain those global mutations with `t.Cleanup` (or the package's existing helper conventions) so they don't leak beyond the test that needs them. Touch ONLY test code in the `jobs` package. Sharp edge, learned in `http/otel_test.go` during the issue-197 work: the default global propagator is a delegator — setting a concrete propagator wires its delegate permanently, so "save and restore the previous propagator" does NOT undo the installation. See `useTraceContextPropagator`/`usePropagators` in `/Users/maragubot/Developer/glue/http/otel_test.go` for the working pattern. Same caveat may apply to the tracer provider delegation — check and handle accordingly. `go test -shuffle on ./...` and `go test -count=3 -race ./jobs` must pass."

**Interpretation:** Add `t.Cleanup` calls around the `otel.SetTracerProvider`/`otel.SetTextMapPropagator` calls at the top of `TestWithTracing` in `/Users/maragubot/Developer/glue/jobs/runner_test.go`, and verify whether the tracer provider has the same permanent-delegation quirk as the propagator before choosing a cleanup strategy.

**Inferred intent:** Stop this one test function from leaking global tracer/propagator state into the rest of the `jobs_test` binary under `-shuffle`, without turning this into a broader refactor of the file.

### What I did

Read `/Users/maragubot/Developer/glue/jobs/runner_test.go` and `/Users/maragubot/Developer/glue/http/otel_test.go` to see the `usePropagators` pattern (lines 665–685 of the latter) and the accompanying comment explaining why "save and restore" doesn't work for the propagator.

To settle whether the tracer provider has the same quirk, I read the vendored source of `go.opentelemetry.io/otel@v1.45.0` (`internal/global/state.go` and `internal/global/trace.go`). Both `SetTracerProvider` and `SetTextMapPropagator` share the identical shape: a `sync.Once` (`delegateTraceOnce` / `delegateTextMapPropagatorOnce`) that, on the *first-ever* call to `Set*` in the process, permanently wires the package's built-in delegating default (`*tracerProvider` / `*textMapPropagator`) to forward to whatever was passed in. Any later `Set*` call just stores a new pointer in an `atomic.Value` and doesn't touch the delegate again. So the danger is specifically: if `TestWithTracing` is the first thing in the `jobs_test` binary to call `otel.SetTracerProvider`, "saving `otel.GetTracerProvider()` beforehand and restoring it afterwards" would restore the *default delegating wrapper* — but that wrapper is now permanently pinned to the SDK provider `TestWithTracing` created, not to true no-op behavior. The same applies to the propagator. Confirmed `go.opentelemetry.io/otel/trace/noop.NewTracerProvider()` exists as the concrete no-op stand-in to install instead of trying to restore "previous".

Edited `/Users/maragubot/Developer/glue/jobs/runner_test.go`:
- Added the `go.opentelemetry.io/otel/trace/noop` import.
- After `otel.SetTracerProvider(tp)`, added `t.Cleanup` that shuts the SDK provider down (`tp.Shutdown(context.WithoutCancel(t.Context()))`, mirroring `oteltest.NewSpanRecorder`'s own cleanup) and then installs `noop.NewTracerProvider()` as the global tracer provider.
- After `otel.SetTextMapPropagator(propagation.TraceContext{})`, added `t.Cleanup` that installs `propagation.NewCompositeTextMapPropagator()` (an empty composite, extracts nothing), exactly the stand-in `usePropagators` uses.
- Added comments explaining the delegator caveat for both, with the propagator one pointing at `usePropagators` in `/Users/maragubot/Developer/glue/http/otel_test.go` for the fuller writeup rather than repeating it.

Did not touch the three subtests further down in `TestWithTracing` that already call `oteltest.NewSpanRecorder(t)` — that helper already scopes its own tracer-provider cleanup per subtest and was out of scope here.

### Why

Global otel state set without cleanup is exactly the kind of thing `-shuffle` is meant to catch: whichever test happens to run afterward in the same binary inherits leftover tracer/propagator state and its outcome becomes order-dependent.

### What worked

Reading the vendored `internal/global` source directly settled the "does the same caveat apply to the tracer provider" question with certainty rather than guesswork — the `SetTracerProvider`/`SetTextMapPropagator` implementations are structurally identical (same `sync.Once`-guarded one-time delegate wiring), so the fix needed the same shape: never rely on "restore what was there before," always install a known-safe stand-in in cleanup, because "before" might have been the pinned default.

`go build ./...`, `go vet ./jobs/...`, `golangci-lint run ./jobs/...` (0 issues), `go test -count=3 -race ./jobs`, and `go test -shuffle on ./...` (and several repeats with `-shuffle on -count=1` targeted at `./jobs ./http ./oteltest ./otel`) all passed cleanly on the first attempt after the edit.

### What didn't work

Nothing failed here — build, vet, lint, and every test run (including race and repeated shuffle runs) passed on the first try after the edit.

### What I learned

`otel.SetTracerProvider` and `otel.SetTextMapPropagator` are backed by literally the same mechanism in `go.opentelemetry.io/otel/internal/global/state.go`: a `sync.Once` per-kind that fires only on the first `Set*` call for that kind in the process and permanently rewires the built-in delegating default. Every `Set*` call after that first one is a plain, fully-reversible store. So "restore the previous value" is only unsafe when the *first* mutation of that global in the process is the one being undone — which a test can't know in advance under `-shuffle`, so the safe rule is: never restore "previous," always install a concrete, known-inert stand-in (`noop.NewTracerProvider()` for the tracer provider, an empty `propagation.NewCompositeTextMapPropagator()` for the propagator).

The codebase already uses `[identifier]`-style doc-link brackets inside ordinary function-body comments, not just above declarations (e.g. `/Users/maragubot/Developer/glue/http/otel_test.go:60,233,667,670`), so using that syntax in the new comments here (`[otel.SetTracerProvider]`) is consistent with existing convention even though it's not a Go doc comment in the strict sense.

### What was tricky

Confirming the tracer-provider half of the caveat required reading OpenTelemetry SDK internals rather than trusting the propagator writeup by analogy — the two mechanisms turned out to be identical in shape, but that had to be verified against `internal/global/state.go` and `internal/global/trace.go` rather than assumed.

### What warrants review

The diff is entirely within `/Users/maragubot/Developer/glue/jobs/runner_test.go` (one new import, cleanup added around the two `otel.Set*` calls at the top of `TestWithTracing`, comments explaining why). To validate: `go test -count=3 -race ./jobs` and `go test -shuffle on ./...` both need to pass, and did. Worth a second look: whether shutting the SDK tracer provider down in cleanup (`tp.Shutdown`, mirroring `oteltest.NewSpanRecorder`) is wanted here or is scope creep beyond the literal ask of "contain the mutations" — I judged it in-scope since it's the same one-line idiom the existing `oteltest` helper already uses for the same kind of provider, and left it in.

### Future work

None anticipated. The `oteltest.NewSpanRecorder` helper's own "save current, restore previous" pattern for the tracer provider (`/Users/maragubot/Developer/glue/oteltest/oteltest.go` lines 24–29) has the identical theoretical exposure if it's ever the first thing in a binary to touch the global tracer provider — out of scope for this task, but worth a future look if it ever surfaces as a real flake.

## Step 3: Fix the root cause in `oteltest` instead of the call site

**Author:** jobs-test-cleanup

### Prompt Context

**Verbatim prompt:** "shouldn't this use the oteltest package? He's right, and the problem is bigger than the call site you fixed. Rework PR 199... Fix the root cause in `oteltest`: make restore-previous safe. Note the nested case — an inner `NewSpanRecorder` must restore the outer's provider, so unconditionally installing noop in cleanup is wrong there. Suggested shape: a package-level `sync.Once` in oteltest that, on first helper use, pins the global tracer provider (and propagator) to inert implementations (noop provider / empty composite) before any save happens, so 'previous' is never the pristine delegator and save/restore becomes always-correct... Add a propagator helper to `oteltest`... and migrate `usePropagators`/`useTraceContextPropagator` in `/Users/maragubot/Developer/glue/http/otel_test.go` to it. Rework `jobs/runner_test.go` to use the `oteltest` helpers instead of hand-rolled globals... Fix or strengthen the vacuous 'restores the previous tracer provider' test... `go test -shuffle on ./...` and `-count=3 -race` on `./jobs ./http ./oteltest ./otel` must pass... this is exported-API surface now, so a proper review pass."

**Interpretation:** Markus (via the lead) spotted that Step 2's fix only patched the one call site in `/Users/maragubot/Developer/glue/jobs/runner_test.go` while `oteltest.NewSpanRecorder` — exported library API, used across the module — carried the exact same "save `otel.GetTracerProvider()`, restore it in cleanup" pattern Step 2 had already proven unsafe for propagators. The ask was to fix the shared helper package itself, add the missing propagator helper there too, migrate both existing call sites (`jobs` and `http`) onto it, and make the regression test in `oteltest_test.go` actually assert behavior instead of pointer identity.

**Inferred intent:** Stop re-deriving the same fix at every call site; put the safety guarantee in the one shared package so every current and future caller of `oteltest.NewSpanRecorder`/a propagator helper gets it for free, and prove the guarantee with tests that would actually fail if it regressed.

### What I did

Re-read `/Users/maragubot/Developer/glue/oteltest/oteltest.go` and `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go`, and confirmed `NewSpanRecorder` did exactly what Markus described: `previous := otel.GetTracerProvider()` before `otel.SetTracerProvider(tp)`, then `otel.SetTracerProvider(previous)` in cleanup.

Before implementing, I wanted to know whether this was actually an *observable* bug for the tracer provider specifically, since the SDK's `sdktrace.TracerProvider.Shutdown()` makes every later `.Tracer()` call on that provider return a no-op tracer, checked live on every call (confirmed by reading `provider.go` in the vendored `go.opentelemetry.io/otel/sdk@v1.45.0`). I wrote a standalone repro at `/private/tmp/.../scratchpad/otelprobe` (a throwaway module, not part of this repo) that reproduced the exact "first-ever `SetTracerProvider` call in the process, save-then-restore in cleanup" scenario against the real SDK. Result: a span started after the restored-but-still-permanently-wired default came back with `valid=false recording=false` — i.e. functionally a no-op, because the provider it silently pointed to was already shut down. So for `NewSpanRecorder` specifically, the existing "shutdown, then restore previous" idiom was accidentally safe in practice, because `Shutdown()` neuters the stale delegate regardless of what the global points at. The propagator has no such self-neutralizing mechanism (no shutdown concept), so that half of Markus's report is a real, currently-inert bug in the tracer-provider case and a real, live bug in spirit for any future propagator helper built the naive way.

I implemented the suggested shape anyway, since it removes reliance on that Shutdown-based accident (a future change to `NewSpanRecorder` that skips or reorders the shutdown call would silently reintroduce a real bug) and unifies the fix for both globals. In `/Users/maragubot/Developer/glue/oteltest/oteltest.go`:
- Added `var pinGlobals = sync.OnceFunc(func() { otel.SetTracerProvider(noop.NewTracerProvider()); otel.SetTextMapPropagator(propagation.NewCompositeTextMapPropagator()) })`, called at the top of both `NewSpanRecorder` and the new `UsePropagators`, before either captures "previous". Because it runs before the first-ever save, "previous" downstream is never the SDK's internal delegating placeholder — it's always this pin or a concrete value a prior helper call installed — so plain save/restore becomes correct everywhere, including the nested case (an inner `NewSpanRecorder`/`UsePropagators` restoring the outer's still-live provider/propagator, which was already correct before this change and remains so).
- Added `func UsePropagators(t *testing.T, propagators ...propagation.TextMapPropagator) propagation.TextMapPropagator`, mirroring `NewSpanRecorder`'s shape: composes the given propagators, installs them globally, registers `t.Cleanup` to restore "previous", and returns the composed propagator for the caller to inspect (`.Fields()` etc.).

Migrated `/Users/maragubot/Developer/glue/http/otel_test.go`'s `usePropagators` to call `oteltest.UsePropagators(t, propagation.TraceContext{}, propagation.Baggage{})` instead of managing the global and cleanup itself, keeping only the package-specific self-check ("expected the propagator under test to carry baggage") inline, now checked against the returned propagator's `.Fields()` rather than re-fetching the global.

Reworked `/Users/maragubot/Developer/glue/jobs/runner_test.go`'s `TestWithTracing`: replaced the Step-2 hand-rolled `sdktrace.NewTracerProvider()` + `otel.SetTracerProvider` + `otel.SetTextMapPropagator(propagation.TraceContext{})` (with the noop/empty-composite cleanup Step 2 added) with a single `oteltest.NewSpanRecorder(t)` + `oteltest.UsePropagators(t, propagation.TraceContext{})` at the top of the test function. Checked every subtest: the first four don't call their own recorder and only assert `span.SpanContext().IsValid()`/trace-ID equality, so a shared, working (non-noop) provider from the top-level call is sufficient — they never inspect what got recorded, so the shared recorder accumulating unread spans doesn't matter. The later three subtests already called their own nested `oteltest.NewSpanRecorder(t)`, which layers correctly on top of the shared one and restores it afterward. Dropped the now-unused `sdktrace` and `noop` imports from the file; `context` stayed, since it's still used for `context.Context` parameter types elsewhere in the file (the `context.WithoutCancel` cleanup call that needed it went away with the hand-rolled setup).

Reworked `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go`'s vacuous `is.Equal(t, previous, otel.GetTracerProvider())` test into two behavioral ones (`"does not record spans through the global tracer after its own cleanup"` and `"restores the outer recorder for a nested test"`), and added `TestUsePropagators` with three behavioral tests (injection works with a real recorder in scope, no leakage after cleanup, correct nested restore). Verified these tests actually discriminate by temporarily removing the `pinGlobals()` calls from `NewSpanRecorder`/`UsePropagators`: with them removed, `go test -v -run '^TestUsePropagators$' ./oteltest/...` failed exactly as expected — `does_not_propagate_through_the_global_propagator_after_its_own_cleanup` got a leaked `traceparent` header (`"00-7d8cb0ab...-01"`) instead of `""` — then restored `pinGlobals()` and confirmed everything passed again. Ran into one test-design gotcha along the way: `propagation.TraceContext{}.Inject` writes nothing for an invalid span context, which every no-op tracer produces, so both propagator tests needed a real `oteltest.NewSpanRecorder(t)` in scope to produce a valid span to inject — the first version of the "injects with the given propagators" test failed for this reason before I added the recorder.

Ran `golangci-lint run` and `gofmt -l` across the four changed packages; `gofmt` flagged an import-ordering issue in `oteltest_test.go` (the new `tracetest` import landed alphabetically before `semconv` instead of after) which `gofmt -w` fixed.

Then dispatched two independent code-review subagents (via the fabrik `code-review` skill's two-competing-reviewer method, since this is now exported library API) with a detailed brief covering the mechanism, the diff, and nine specific things to check per reviewer, including a mandatory "read every new/changed comment as an outside reader" pass for context leakage. Both reviewers independently ran their own builds/tests/races and one wrote its own standalone repro to verify the `otel` SDK's delegate-wiring mechanics directly against the vendored source, rather than trusting the brief. Both converged on the same moderate finding: the `pinGlobals` doc comment (and two echoing comments in `oteltest_test.go`) cited `[otel.SetTracerProvider]`/`[otel.SetTextMapPropagator]` via doc-link syntax as though their public godoc documented the permanent-delegate-wiring behavior, when that behavior is actually undocumented, internal to `go.opentelemetry.io/otel/internal/global`, and could change without notice in a future otel release without being flagged as a breaking API change. I fixed this by rewording the `pinGlobals` comment to say plainly that this is "an internal detail of that module, not a documented guarantee of `[otel.SetTracerProvider]` or `[otel.SetTextMapPropagator]`, but observably true of the versions this module depends on," and softened the two test-file echoes to say "the SDK's internal placeholder" instead of implying it was a documented default. One reviewer additionally raised (single-reviewer, not corroborated) a real but pre-existing and currently-inert SDK sharp edge: a `trace.Tracer` obtained via `otel.Tracer(name)` and cached on a struct field *before* the very first `SetTracerProvider` call in a process becomes permanently deaf to later provider changes — true independent of this fix, confirmed not currently triggered anywhere in the repo (`jobs.WithTracing`, `s3.NewBucket`, etc. are never constructed before `oteltest` helpers run in the same test binary), and not something `pinGlobals` introduces or worsens, so I left it undocumented in code and noted it here instead. The same reviewer's naming nitpick (`UsePropagators` vs. matching `NewSpanRecorder`'s `NewX` convention) was single-reviewer, non-serious, and the coordinator's own request had explicitly floated "`UsePropagators`... or similar naming" as an acceptable option, so I kept the name as-is.

After the comment fixes, reran `go build ./...`, `go vet ./...`, `gofmt -l`, `golangci-lint run` (0 issues) on all four changed packages, `go test -count=3 -race ./jobs ./http ./oteltest ./otel`, and `go test -shuffle on ./...` (full suite) — all clean.

### Why

A shared test helper is exactly the place a global-state safety fix belongs: fixing it once in `oteltest` protects every current and future caller, instead of every caller having to independently discover and re-derive the same fix Step 2 worked out for one call site.

### What worked

Writing a throwaway repro against the real SDK (twice: once to characterize whether the tracer-provider case was actually observable, once more implicitly via the deliberate "remove `pinGlobals`, watch the test fail" check) turned "I believe this fix is correct" into "I watched this fix's absence produce the exact leaked `traceparent` the bug report predicted, and watched it disappear when the fix went back in." That's a much stronger validation than reasoning about `sync.Once` semantics from the SDK docs alone. Dispatching two independent reviewers with a detailed technical brief (rather than a generic "review this diff") meant both did real verification work — one traced the exact vendored source for `SetTracerProvider`, the other wrote its own independent repro — and their convergence on the same comment-accuracy issue, despite differing focus otherwise, made that finding easy to trust and fix confidently.

### What didn't work

The first draft of `TestUsePropagators`'s "injects with the given propagators" test failed with `Expected true, but got false` / `expected a traceparent header to be injected`, because it started a span with no real tracer provider active, so the span context was invalid and `propagation.TraceContext{}.Inject` silently wrote nothing regardless of which propagator was installed. Fixed by adding `oteltest.NewSpanRecorder(t)` alongside `oteltest.UsePropagators(t, ...)` in that test. The same class of mistake showed up in the "does not propagate... after its own cleanup" test's first draft too, caught before running it this time by reasoning through the same gotcha, and fixed by adding a recorder scoped to the whole test (not the inner sub-test) so the outer injection always has a valid span regardless of the inner cleanup.

### What I learned

`sdktrace.TracerProvider.Shutdown()` checks `isShutdown` live on every `.Tracer()` call (confirmed in `provider.go` of the vendored SDK, not cached at shutdown time against already-issued `Tracer` values only), so a shut-down provider is self-neutralizing no matter how many old references or delegate wirings still point at it — this is why the pre-existing `NewSpanRecorder` "shutdown then restore previous" pattern never actually manifested as a bug in practice, even though it relied on an implementation detail the coordinator was right to distrust. `propagation.TraceContext{}.Inject` is a no-op for an invalid span context, which is exactly what any no-op tracer produces — so any test asserting propagator *injection* behavior needs a real recording tracer in scope, or it passes (or fails) for the wrong reason regardless of which propagator is actually active.

### What was tricky

Distinguishing "this bug is real and worth fixing the way the coordinator described" from "this bug happens to not currently manifest for this specific case" required going past the propagator-only precedent in `/Users/maragubot/Developer/glue/http/otel_test.go` and actually reading the SDK's `Shutdown()` implementation and reasoning through a repro — the temptation was to pattern-match "propagator was unsafe, so tracer provider must be too" without checking whether the two globals' failure modes were actually equivalent (they weren't, because of `Shutdown()`'s live check), even though implementing the requested fix was correct regardless of that nuance.

### What warrants review

The diff spans `/Users/maragubot/Developer/glue/oteltest/oteltest.go`, `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go`, `/Users/maragubot/Developer/glue/http/otel_test.go`, and `/Users/maragubot/Developer/glue/jobs/runner_test.go`. `oteltest.go` is exported library API now carrying a new public function (`UsePropagators`) and a new package-level `sync.OnceFunc` (`pinGlobals`) that every test in the module touching these globals implicitly depends on being called first — worth confirming no code outside `oteltest.go` calls `otel.SetTracerProvider`/`otel.SetTextMapPropagator` directly (both reviewers independently grepped and confirmed none does, as of this diff). To validate: `go test -count=3 -race ./jobs ./http ./oteltest ./otel` and `go test -shuffle on ./...` both need to pass, and did, repeatedly. The two-reviewer report is summarized above; both are satisfied, no serious or corroborated-moderate issues remain open.

### Future work

The single-reviewer finding about pre-first-call `otel.Tracer(name)` caching being permanently deaf to later provider changes is real (verified independently by that reviewer with its own repro) but not caused or worsened by this change, not currently triggered anywhere in this repo, and orthogonal to `pinGlobals`'s guarantee (it's about *tracer objects obtained before pinning fires*, not about the globals themselves) — worth keeping in mind if a future package ever constructs something that caches `otel.Tracer(...)` at package-init time or before test setup runs, but not actionable today.

## Step 4: Replace `pinGlobals` with a switchable stand-in, after an external review found it broke valid library usage

**Author:** jobs-test-cleanup

### Prompt Context

**Verbatim prompt:** "An external second-opinion review (OpenAI codex, strict definite-bugs mode) found real problems with the pinGlobals design in PR 199 — the design I suggested, so blame lands here, but they need fixing. Markus has accepted them as valid; fix by judgment, push to the same branch. He is NOT done reviewing, so do not merge. Finding 1 (P2): pinGlobals unconditionally overwrites both globals at first helper use. A consumer who configured a real provider or propagator before the first helper call (e.g. in TestMain) gets it silently destroyed... Finding 2 (P2): a tracer obtained and cached before the first Set* in the process... is permanently bound to whatever is set FIRST. Pre-PR that was the recorder's provider, so cached tracers recorded; now the pin makes it noop... Finding 3 (P3): the pinGlobals comment block and oteltest_test.go comments lean on undocumented internal SDK behavior and reference the unexported pinGlobals from another package's perspective... Design direction — your call, but constraints that must hold, each backed by a test: a. A provider/propagator configured before the first helper call is restored after cleanup... b. A tracer cached before any helper call still delivers spans to the active recorder, or... its spans at minimum go nowhere after cleanup AND the limitation is documented... c. Nested helper calls restore correctly... d. In a pristine process, nothing leaks after cleanup... Codex suggested a 'switchable pinned provider'... watch the pristine case: the pre-pin 'previous' is the delegator, and using it as the switchable's target creates a forwarding cycle."

**Interpretation:** The `pinGlobals` design from Step 3 — unconditionally installing an inert no-op/empty-composite as the very first `otel.Set*` call, before capturing anything as "previous" — was itself a library-correctness bug: it assumed no consumer would ever configure a real provider/propagator before an `oteltest` helper first ran, and assumed nothing would ever cache a `trace.Tracer` before that point either. Both assumptions are exactly the kind CLAUDE.md warns against ("don't justify code decisions by assuming how consumers use it"). This step redesigns the fix so both are handled correctly, with each constraint backed by its own test, and fixes the Finding 3 comment issue by rewording rather than by silence.

**Inferred intent:** Ship a version of this fix that is actually safe for arbitrary valid library usage, not just for this repo's own test suite as it exists today — and prove each safety property with a test that would fail if the property regressed, rather than asserting it in a comment.

### What I did

Worked out the design by hand before writing code, since Codex's suggested shape ("switchable pinned provider... watch the pristine case: the pre-pin 'previous' is the delegator... creates a forwarding cycle") needed the exact mechanics nailed down to implement safely. Re-read `go.opentelemetry.io/otel@v1.45.0`'s `internal/global/state.go` and `internal/global/trace.go` again (already read in Step 3) and additionally read `internal/global/propagator.go` for the first time, confirming the propagator's raw default (`textMapPropagator`) has the identical one-time `sync.Once`-guarded `SetDelegate` shape as the tracer provider's, and that both resolve their delegate *live* on every call (`effectiveDelegate()` for the propagator, a `nil`-check-then-forward in `tracerProvider.Tracer()` for the provider) rather than caching it once at wiring time. That "resolves live" property is exactly what makes a custom, permanently-installed stand-in workable: whatever the SDK's raw default binds its delegate to (once, forever) doesn't matter as long as that binding target itself keeps re-resolving to wherever a test currently wants spans/propagation to go.

Landed on this shape:
- `pristineTracerProvider`/`pristinePropagator` (`/Users/maragubot/Developer/glue/oteltest/oteltest.go`): captured via plain package-level `var` initializers, which Go runs before any test function or a consumer's own `TestMain` in this package's own init sequence. These are the baseline `install`'s first-run decision compares the current global against.
- `switchableTracerProvider` / `switchableTracer` / `switchablePropagator` (new file `/Users/maragubot/Developer/glue/oteltest/switchable.go`): small hand-written types implementing `trace.TracerProvider`, `trace.Tracer`, and `propagation.TextMapPropagator` respectively, each holding a mutex-guarded `target` field that every method call resolves live via `getTarget()` rather than fixing at construction time. `switchableTracerProvider.Tracer(name)` returns a `*switchableTracer` (not a direct call into the current target), specifically so that a `.Start()` call made long after `.Tracer(name)` was obtained still re-resolves the provider's *current* target rather than whatever was current when `.Tracer()` was called — this is the piece that makes a pre-cached `trace.Tracer` follow a later `NewSpanRecorder` swap.
- `install` (`var install = sync.OnceFunc(doInstall)`, with the body pulled into a standalone `doInstall()` so a test can re-run it under controlled conditions): on first use, decides each stand-in's *starting* target by comparing `otel.GetTracerProvider()`/`otel.GetTextMapPropagator()` against the pristine snapshots. If they differ, something (a consumer's `TestMain`, most plausibly) already configured a real value — that becomes the stand-in's starting target, fixing Finding 1. If they're equal (nothing has touched the global since package init), the starting target defaults to a fresh inert stand-in (`noop.NewTracerProvider()`, an empty composite propagator) instead of the still-pristine raw default itself — using the raw default as the switchable's own target would, once `otel.SetTracerProvider(globalTracerProvider)` wires that raw default's one-time delegate to the switchable, make the switchable's own target resolution recurse into itself forever, exactly the "forwarding cycle" Codex flagged.
- Crucially, `install()`'s two `otel.SetTracerProvider`/`otel.SetTextMapPropagator` calls are now the *only* calls this package ever makes to those functions, for the life of the process. `NewSpanRecorder`/`UsePropagators` no longer call them at all — they call `globalTracerProvider.setTarget(...)`/`globalPropagator.setTarget(...)` instead, a plain mutex-guarded field assignment with none of the SDK's one-time-wiring behavior anywhere near it. This is what makes "restore previous" provably safe now, for any nesting depth, without needing to reason about SDK internals at each call site the way Step 3's `pinGlobals` still implicitly did.

Wrote the tests requirement (a)-(d) explicitly asked for, each in the file matching what it needs to touch:
- `/Users/maragubot/Developer/glue/oteltest/oteltest_internal_test.go` (new, `package oteltest`, white-box): a `withFreshInstall(t)` helper that resets `install` to a fresh `sync.OnceFunc(doInstall)` and the pristine snapshots to whatever's currently active, restoring everything (the real global otel state plus this package's own bookkeeping vars) on cleanup — needed because `install` is only ever supposed to fire once per process, so testing its first-run decision more than once in one test binary requires deliberately re-arming it. `TestInstall` uses this to test (a) directly: set a real provider/propagator via `otel.SetTracerProvider`/`otel.SetTextMapPropagator` (simulating a `TestMain`), run a nested `NewSpanRecorder`/`UsePropagators`, and assert the real one is back and working after the nested test's own cleanup. Also tests (d)'s "nothing has configured anything" branch explicitly, confirming the cycle-avoidance path.
- `/Users/maragubot/Developer/glue/oteltest/switchable_internal_test.go` (new, `package oteltest`, white-box): direct, cheap unit tests of `switchableTracerProvider`/`switchablePropagator`'s "resolves live" property in isolation, independent of the SDK's global one-time-wiring machinery.
- `/Users/maragubot/Developer/glue/oteltest/pristine_test.go` (new, `package oteltest_test`): `TestPreCachedTracerFollowsANewSpanRecorder` and `TestPreCachedPropagatorFollowsUsePropagators`, testing (b) end-to-end for real. Neither `install`'s own `sync.Once` nor the SDK's internal one-time delegate wiring can be reset mid-process, so "a tracer/propagator obtained before anything in the process has ever touched otel" can only be tested in a genuinely fresh process — both tests re-exec the test binary as a subprocess (`exec.Command(os.Args[0], "-test.run=^TestX$", "-test.v")`, guarded by an `OTELTEST_PRISTINE_SUBPROCESS` env var to avoid infinite recursion), obtain the tracer/propagator reference first thing in that fresh subprocess, then call `oteltest.NewSpanRecorder`/`UsePropagators` and confirm the pre-obtained reference's spans/injections land correctly.
- `TestNewSpanRecorder`/`TestUsePropagators` in `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go` (from Step 3, kept as-is apart from two reworded comments) already cover (c) and (d)'s "leaves nothing reachable" half.

Verified every new test actually has teeth, not just that it passes, by deliberately breaking the corresponding piece of the fix and confirming red-then-green each time: reverted `doInstall`'s pristine comparison to unconditionally use the inert stand-in (Finding 1's exact original bug) and watched both `TestInstall` sub-tests fail with the right messages; reverted `switchableTracerProvider.Tracer()` to resolve the target immediately (`return p.getTarget().Tracer(name, opts...)`, the naive/buggy shape) and watched both `TestSwitchableTracerProvider` and the `pristine_test.go` tracer subprocess test fail; reverted `UsePropagators` to call `otel.SetTextMapPropagator(p)` directly instead of `globalPropagator.setTarget(p)` and watched the new propagator subprocess test fail with a real, useful message ("expected the pre-cached propagator to inject with the configured propagator"). Restored the real code after each check and reran the full suite to confirm green again.

Added `var _ trace.TracerProvider = (*switchableTracerProvider)(nil)` / `var _ trace.Tracer = (*switchableTracer)(nil)` / `var _ propagation.TextMapPropagator = (*switchablePropagator)(nil)` compile-time assertions in `switchable.go`, matching the convention the OpenTelemetry SDK's own internal package uses for the equivalent types.

Addressed Finding 3 by rewording every comment that either asserted internal, undocumented OpenTelemetry SDK mechanics as established fact, or named an unexported identifier (`pinGlobals`, from Step 3) from a different package's perspective (`oteltest_test.go`'s comments referenced it, unresolvable from that external test package). Reworded `install`'s doc comment in `oteltest.go` to describe *observable behavior* ("can go on affecting later calls even after being 'restored' away", "can fail to pick up any later one at all") instead of asserting the SDK's internal delegate-wiring mechanism as documented fact; did the same for `switchable.go`'s two type doc comments; simplified the two `oteltest_test.go` comments that named `pinGlobals` down to describing what each sub-test's ordering trick achieves, with no claim about *why* it matters at the SDK level.

Then ran the two-competing-subagent review (`fabrik:code-review`) a second time, since this redesign is a substantial rewrite of exported API surface with real concurrency and correctness stakes. Both reviewers independently verified the SDK mechanics against the vendored `go.opentelemetry.io/otel@v1.45.0` source themselves rather than trusting the code's comments, ran the full test matrix (`-race`, `-count=3`, `-shuffle`, several repeats) themselves, and traced through the "would this test actually fail if the fix were reverted" question for each new test by hand. Neither found a bug. Findings, and what I did about each:
- Reviewer A (single-reviewer, moderate): `withFreshInstall`'s doc comment claimed the reset is "invisible to any other test sharing the process," which overclaims — it cannot undo the OpenTelemetry SDK's own process-global one-time delegate wiring for a `trace.Tracer` some *other* test might have cached before the reset ran. Not currently triggered (nothing in this repo does that), but the comment was still asserting more than is true. Reworded to say plainly what it can't undo.
- Reviewer A (nitpick): the `pristineTracerProvider`/`pristinePropagator` doc comment said "Go guarantees runs before any test... or a consumer's own `TestMain`," which is broader than what Go's spec actually guarantees — it guarantees dependency-ordered initialization within this package, not ordering relative to an unrelated sibling package's own `init()`. Reworded to scope the claim correctly and note the (very unlikely) gap explicitly rather than pretend it doesn't exist.
- Reviewer B (moderate, and the one I judged worth acting on despite being single-reviewer, since it's a real coverage gap in the exact mechanism this whole step is about): the subprocess test only covered Finding 2 for the tracer-provider case, not the symmetric propagator case — nothing in the suite would catch a regression in `switchablePropagator`'s "resolves live" behavior specifically via the real SDK integration path (only the cheap white-box unit test would, which tests the mechanism in isolation, not the full integration through the SDK's own delegate wiring). Added `TestPreCachedPropagatorFollowsUsePropagators` to `pristine_test.go`, verified it fails when I break the propagator side of the fix the same way I'd verified the tracer one.
- Reviewer B (nitpick): the residual, genuinely unfixable limitation — a consumer who both caches a `trace.Tracer`/propagator reference *and* configures the real otel globals themselves, all before any `oteltest` helper ever runs — was true but undocumented. Added a short, honest paragraph to `install`'s doc comment naming this boundary explicitly rather than leaving it implicit, per the instruction to document a corner honestly rather than half-fix it.

I did not act on: Reviewer A's nitpick about `current != pristineTracerProvider` being a theoretical interface-equality panic risk (verified safe today since both real otel default types are pointers, hence always comparable; single-reviewer, not corroborated, and the risk mode is "panics loudly on every test" rather than silent corruption, which is a fail-safe direction) — left as is per the code-review skill's signal-to-noise guidance for uncorroborated nitpicks; Reviewer B's confirmation-only note about the cycle risk being real (not an issue, just independent verification that the special-casing is load-bearing).

After all edits, reran `go build ./...`, `go vet ./...`, `gofmt -l` (clean), `golangci-lint run` (0 issues) on `/Users/maragubot/Developer/glue/oteltest`, `/Users/maragubot/Developer/glue/jobs`, `/Users/maragubot/Developer/glue/http`, `go test -count=3 -race ./jobs ./http ./oteltest ./otel`, `go test -shuffle on ./...` (full suite), and five more repeats of `go test -shuffle on -count=1 ./jobs ./http ./oteltest ./otel` — all green throughout.

Updated the `docs/decisions.md` entry from Step 3 is superseded by a fresh decision entry for this step (per the decisions skill's rule against rewriting past entries), recording the switchable-provider design and why the simpler pin was replaced.

### Why

The whole point of Step 3's redesign was to stop relying on implementation accidents (like `Shutdown()` happening to neuter a leaked tracer provider) and put a provably-correct guarantee in one shared place — but `pinGlobals` itself turned out to rest on an unstated assumption (nothing configures otel or caches a tracer before the first `oteltest` call) that isn't actually guaranteed by anything, and CLAUDE.md is explicit that a library must be designed and reviewed for correctness across all valid uses of its public API, not just the ones exercised by this repo's own tests today.

### What worked

Working out the exact mechanics by hand *before* writing code — specifically, confirming from the vendored SDK source that both the tracer provider's and the propagator's raw defaults resolve their one-time-bound delegate *live* on every call — turned "install a switchable stand-in" from a plausible-sounding idea into a design I could reason about with certainty, including correctly predicting and avoiding the exact forwarding-cycle failure mode Codex's prompt warned about, before ever running the code. Deliberately breaking each piece of the fix and watching the corresponding test go red (four separate break/verify/restore cycles this step: Finding 1's pristine-check, the tracer switchable's live-resolution, the propagator switchable's live-resolution via a realistic regression at the `UsePropagators` call site) caught nothing new by this point, but converted "I believe these tests are meaningful" into "I watched each one fail for the right reason," which is exactly the standard this whole multi-step effort has been trying to hold itself to since Step 1.

### What didn't work

My first attempt to verify the new propagator subprocess test's teeth was wrong: I tried breaking `switchablePropagator.Inject` by inlining `p.target.Inject(...)` instead of `p.getTarget().Inject(...)`, expecting this to simulate a "resolved once" bug the way skipping `getTarget()` did for the tracer case. It didn't — `p.target` is still read fresh on every call in Go (there's no caching from removing a mutex-protected accessor method), so this change only removed race-safety, not correctness, and the test kept passing for the wrong reason (I caught this by reasoning about it before running it, not by a red run). The propagator case has no equivalent to the tracer provider's `.Tracer(name)` / `.Start()` two-step split — `switchablePropagator`'s methods are the terminal call, not an intermediate object a caller might hold onto — so the actually-representative regression to test against was different: making `UsePropagators` call `otel.SetTextMapPropagator(p)` directly instead of `globalPropagator.setTarget(p)`, which does correctly reproduce a real version of the SDK-level bug (the raw default's one-time delegate would stay wired to whichever provider happened to be first, and a pre-cached reference wouldn't observe the later direct `Set` call). That break did fail the test as expected.

### What I learned

The OpenTelemetry SDK's internal `textMapPropagator` (in `internal/global/propagator.go`) mirrors the tracer provider's `sync.Once`-guarded one-time delegate wiring exactly, including the "resolves the delegate live on every call once set" property — this was worth confirming directly rather than assuming by analogy, since it's what makes a permanently-installed switchable stand-in workable for propagators at all, not just for tracer providers. A `trace.Tracer` obtained from the SDK's un-configured raw default provider is not a live view of "the current global tracer" in the way `otel.GetTracerProvider()` is — it's a *specific*, individually-tracked placeholder object (`internal/global/trace.go`'s `p.tracers` map, keyed by name/options) that gets retargeted exactly once, whenever the very first delegate gets wired, and never again on its own — which is precisely why a switchable *provider* handing out switchable *tracers* (rather than a switchable provider that resolves everything itself) is the right shape: the indirection has to live one level below the one-time-wiring point, not at it.

### What was tricky

Distinguishing "this needs its own regression test" from "this is a property that's obviously true by construction" for the propagator case, once I'd already built the tracer-provider version and had a template in mind. The propagator's mechanism is genuinely simpler (no intermediate object, no `.Tracer()`/`.Start()` split) which made it tempting to assume the same test shape would carry over unchanged — it took actually trying to break it the analogous way, watching that fail to demonstrate anything, and reasoning through what a *real* regression at this call site would look like instead, to land on a test that's actually representative.

### What warrants review

New files: `/Users/maragubot/Developer/glue/oteltest/switchable.go`, `/Users/maragubot/Developer/glue/oteltest/oteltest_internal_test.go`, `/Users/maragubot/Developer/glue/oteltest/switchable_internal_test.go`, `/Users/maragubot/Developer/glue/oteltest/pristine_test.go`. Modified: `/Users/maragubot/Developer/glue/oteltest/oteltest.go` (the `install`/`doInstall` bootstrap replaces Step 3's `pinGlobals`; `NewSpanRecorder`/`UsePropagators` now swap a stand-in's target instead of calling `otel.Set*` again), `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go` (two comments reworded per Finding 3, test bodies otherwise unchanged from Step 3). `http/otel_test.go` and `jobs/runner_test.go` are untouched this step — they already call the public `oteltest.NewSpanRecorder`/`oteltest.UsePropagators` API from Step 3 and needed no changes for this redesign. Markus is explicitly not done reviewing (per this step's prompt) — do not merge PR 199 on the strength of this step alone. To validate: `go test -count=3 -race ./jobs ./http ./oteltest ./otel` and `go test -shuffle on ./...`, both of which pass repeatedly as of this step. Worth a specific look: whether the subprocess-re-exec test pattern in `pristine_test.go` (new to this codebase, as far as I found) is an acceptable idiom here, or whether the coordinator would prefer a lighter-weight approach despite the "each constraint backed by a test" requirement essentially forcing this shape for constraint (b)'s true end-to-end case.

### Future work

None anticipated beyond what Markus's continued review turns up.

## Step 5: Reconcile on every helper call instead of once, after a second review round found the switchable could still detach

**Author:** jobs-test-cleanup

### Prompt Context

**Verbatim prompt:** "Second codex review round (fresh session) on the redesigned PR 199 branch: one P2, one P3. Both accepted under Markus's standing 'fix by judgment'. Still do not merge. P2 (I verified against oteltest.go): `install` being a spent OnceFunc means a consumer calling `otel.SetTracerProvider`/`otel.SetTextMapPropagator` AFTER the first helper has run detaches the switchables — the global becomes the consumer's value, later helpers mutate a wrapper nothing reads, and the recorder silently receives no spans. You handled config-before-first-helper; this is config-after. Fix: on every helper entry, re-detect — if the current global is not this package's switchable, reinstall it, adopting the current global as the starting target (a later Set* is a plain store, so reinstalling is safe). This generalizes the install-time adoption; consider whether install()'s once-ness can collapse into the same reconcile-on-entry path instead of two mechanisms. Test it: helper → consumer Set* → helper again → recorder sees spans, and cleanup restores the consumer's value. P3: the `install` doc comment (oteltest.go:53-55 area) and `switchablePropagator` comments narrate consumer scenarios (TestMain etc.) and undocumented SDK behavior. Reword as local invariants — what this package guarantees and why in terms of its own state; caller scenarios and SDK-internals rationale belong in the diary, which already has them."

**Interpretation:** Step 4's `install` (a `sync.OnceFunc`) only ever ran once per process. It correctly handled a consumer configuring the real otel globals *before* the first `oteltest` helper call (Step 4's finding 1 fix), but had no way to notice if a consumer configured them again *after* that first call — the spent `sync.Once` would never re-fire, so `globalTracerProvider`/`globalPropagator` would silently keep mutating a stand-in that was no longer the active global, and `NewSpanRecorder`'s recorder would receive nothing. The fix generalizes Step 4's first-attachment logic into something that runs on every helper call, not just the first.

**Inferred intent:** Close the remaining gap in the "detach" direction so the guarantee holds regardless of when or how many times a consumer's own code touches the otel globals relative to `oteltest`'s own calls, and make the code comments describe what this package itself guarantees rather than narrating who might call it or why the SDK behaves the way it does.

### What I did

Worked out the collapse the prompt suggested by hand first. The key realization: once *any* `otel.SetTracerProvider`/`otel.SetTextMapPropagator` call has ever happened in the process — ours or a consumer's — every later call to those functions is a plain, reversible store; the SDK's one-time delegate-wiring already fired on whichever call was first, and nothing after that is special. So the "is this the very first call ever, and do I need to worry about wiring a self-referencing cycle" question only matters for deciding a *brand new* stand-in's starting target, not for the act of reinstalling one that already exists. That meant the fix could reuse the *same* `globalTracerProvider`/`globalPropagator` object across any number of detach/reattach cycles, rather than constructing a new one each time — which mattered, because a `trace.Tracer` the SDK had already bound (permanently) to that object during the process's actual first-ever `Set` call would only keep working if the object it was bound to kept being the one whose target gets updated. Replacing it with a fresh object on reattachment would have silently reintroduced Step 4's finding 2 for the "detach after first attachment" case.

Rewrote `/Users/maragubot/Developer/glue/oteltest/oteltest.go`: removed `install`/`doInstall`/the `sync.OnceFunc`/the `sync` import entirely, replacing them with a single `ensureAttached()` function called at the top of both `NewSpanRecorder` and `UsePropagators` on every call. It lazily creates `globalTracerProvider`/`globalPropagator` once (if `nil`) with a throwaway placeholder target, then for each one: compares `otel.GetTracerProvider()`/`otel.GetTextMapPropagator()` against the stand-in itself; if they match, the stand-in is still attached and there's nothing to do; if they don't, something detached it (first-ever call, or a later consumer `Set*`) and it reattaches, adopting whatever is current as the new target unless that current value is still the untouched `pristineTracerProvider`/`pristinePropagator` snapshot, in which case it falls back to a fresh inert stand-in instead (the cycle-avoidance case from Step 4, unchanged). `NewSpanRecorder`/`UsePropagators` themselves didn't need to change beyond calling `ensureAttached()` instead of `install()` — they already only ever touched `globalTracerProvider.setTarget(...)`, never `otel.SetTracerProvider` directly, so the reconciliation happening on every call rather than once just meant they always start from a genuinely-current stand-in.

Added the requested test to `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go`: `TestNewSpanRecorder`'s `"reattaches after a consumer replaces the global tracer provider directly, restoring the consumer's provider on cleanup"` and the symmetric `TestUsePropagators` one. Both follow the literal shape asked for — helper call, then a direct `otel.SetTracerProvider`/`otel.SetTextMapPropagator` call simulating something other than this package taking over, then a *nested* nested helper call so its own cleanup fires before the parent test body's final assertion, which checks that the directly-set provider/propagator is back in control afterward, not the reattached stand-in's now-defunct target.

Updated `/Users/maragubot/Developer/glue/oteltest/oteltest_internal_test.go`: renamed `withFreshInstall`/`TestInstall` to `withFreshAttachment`/`TestEnsureAttached` to match, and simplified the reset helper since there's no more `install` var to save and restore — it now just nils `globalTracerProvider`/`globalPropagator` (forcing `ensureAttached` to recreate them fresh) alongside resetting the pristine snapshots, restoring everything in cleanup as before.

Addressed P3 by rewriting `ensureAttached`'s doc comment, the two package var-block comments above it, `NewSpanRecorder`/`UsePropagators`'s doc comments, and both `switchableTracerProvider`/`switchablePropagator`'s type comments in `/Users/maragubot/Developer/glue/oteltest/switchable.go` to describe local invariants -- what each piece guarantees about this package's own state -- rather than narrating an external scenario ("a consumer's own TestMain") or restating SDK internals as background. Kept one exception deliberately: `ensureAttached`'s comment still says "`[otel.SetTracerProvider]` and `[otel.SetTextMapPropagator]` can be called by anything, at any time" — decided this describes the *public, documented* nature of the dependency's own API surface (any caller can call an exported function at any time is true of any exported function, not a claim about undocumented internals or a specific caller), so it stays on the right side of the CLAUDE.md rule about referencing a dependency's public contract. Kept a trimmed version of Step 4's "one case is out of reach" boundary note too, reworded to state the boundary in terms of what `[globalTracerProvider]`/`[globalPropagator]` themselves can reach, rather than narrating who obtained a reference and when -- P3 asked to reword these comments, and dropping the content outright rather than rewording it would have been the wrong read of that instruction. Fixed a few plain-text references to `globalTracerProvider`, `globalPropagator`, `pristineTracerProvider`, and `pristinePropagator` in the new comment to use `[identifier]` doc-link syntax during a final self-review pass, matching the project's own convention for referencing unexported identifiers in the same package.

Verified the new reattachment tests actually have teeth by temporarily short-circuiting `ensureAttached` to return early once `globalTracerProvider` was already non-nil (simulating the exact spent-`sync.Once` bug this step fixes) and confirming both new tests failed with the right messages (`"expected the reattached recorder to receive spans"`, `"expected the reattached propagator to be active"`), along with a cascade into a couple of unrelated tests that happened to run afterward in the same short-circuited state -- itself a small extra confirmation that the bug's blast radius (silently starving every helper call after the first consumer interference) is as bad as P2 described. Reverted and reran the full suite green.

Self-reviewed alone rather than dispatching the two-competing-subagent `fabrik:code-review` process, per this step's explicit instruction that one pass is fine for a diff this size. Walked the `ensureAttached` logic by hand for every case (first-ever call, still-attached no-op call, detach-then-reconcile, detach-to-pristine which can't actually happen post-first-call), checked doc-comment quality and link syntax, and read the two new tests' assertions against what they're meant to prove. Found and fixed the doc-link nitpick above; found nothing else.

Ran `go build ./...`, `go vet ./...`, `gofmt -l`, `golangci-lint run` (0 issues) on `/Users/maragubot/Developer/glue/oteltest`, `/Users/maragubot/Developer/glue/jobs`, `/Users/maragubot/Developer/glue/http`, `go test -count=3 -race ./jobs ./http ./oteltest ./otel`, `go test -shuffle on ./...` (full suite), and five more repeats of `go test -shuffle on -count=1 ./jobs ./http ./oteltest ./otel` -- all green throughout, both before and after the self-review's doc-link fix.

Updated the `docs/decisions.md` 2026-08-26 entry in place again (still editable, created this session) to describe the reconcile-on-every-call design rather than the once-only `pinGlobals`/`install` shape from Steps 3 and 4.

### Why

A one-time install only protects the "before" half of the timeline; a test helper that mutates global process state has no way to know whether it's still the thing anyone's actually looking at unless it checks every time, and the whole point of this multi-round effort has been to stop leaving any half of that timeline to chance.

### What worked

Recognizing that "a later `Set*` is a plain store" (the prompt's own framing) meant the *object identity* problem -- not just the *target value* problem -- was the one that mattered for correctness here. It would have been easy to write a `ensureAttached` that just constructs a brand-new `switchableTracerProvider` whenever it notices a detachment, which reads as simpler and would have passed every test from Step 4 without changes, but would have silently broken the case Step 4 specifically built pre-cached-tracer support for for. Reusing the existing object and only ever moving its target is the detail that keeps both guarantees (survives detachment, survives having been cached against beforehand) true at once, and it only stood out because I traced through what the SDK's one-time delegate wiring actually does to a *specific object reference* rather than reasoning about "the switchable" as an abstract, interchangeable stand-in.

### What didn't work

Nothing failed here in the sense of a wrong turn that needed backing out of -- the design came together on the first attempt once the object-identity point was clear, and the deliberate "break it, confirm red, fix it, confirm green" check on the first attempt passed cleanly in both directions.

### What I learned

Collapsing "install once" into "reconcile every time" was strictly simpler code, not just a fix for the gap -- Step 4's separate `install`/`doInstall`/`sync.OnceFunc` scaffolding existed only to make the once-ness explicit, and once reconciliation has to happen on every call anyway (because attachment can be lost at any time), the once-ness stops being useful for anything and the `sync` import disappears along with it. The prompt's instruction to "consider whether install()'s once-ness can collapse into the same reconcile-on-entry path instead of two mechanisms" turned out to be true in a stronger sense than "consider" suggested -- it wasn't a possible simplification to weigh, it was the only design that doesn't need two separate pieces of state (a spent flag, and a possibly-stale stand-in) to reason about instead of one.

### What was tricky

Deciding what to do with Step 4's "one case is out of reach regardless" paragraph when rewriting `ensureAttached`'s doc comment for P3 -- my first pass dropped it entirely along with the consumer-scenario language it was wrapped in, and it took a second look to notice that P3 asked for a *reword*, not a deletion, and that the underlying boundary (a reference bound directly to something else before either stand-in existed is out of either stand-in's reach) is still true and still worth documenting, just statable without naming who might hold such a reference or when.

### What warrants review

Modified: `/Users/maragubot/Developer/glue/oteltest/oteltest.go` (the `install`/`doInstall`/`sync.OnceFunc` triad is gone, replaced by `ensureAttached`), `/Users/maragubot/Developer/glue/oteltest/oteltest_internal_test.go` (renamed to match, reset helper simplified), `/Users/maragubot/Developer/glue/oteltest/oteltest_test.go` (two new reattachment tests), `/Users/maragubot/Developer/glue/oteltest/switchable.go` (two doc comments reworded), `/Users/maragubot/Developer/glue/oteltest/switchable_internal_test.go` (one stale `[install]` doc-link fixed to `[ensureAttached]`). Markus is still reviewing (per this step's prompt, again) -- do not merge PR 199 on the strength of this step. To validate: `go test -count=3 -race ./jobs ./http ./oteltest ./otel` and `go test -shuffle on ./...`, both passing repeatedly as of this step. Worth a specific look: whether reusing the object across reattachments (rather than constructing a fresh one, which reads simpler) is explained clearly enough in `ensureAttached`'s doc comment for a future maintainer to not "simplify" it back into a bug.

### Future work

None anticipated beyond what Markus's continued review turns up.
